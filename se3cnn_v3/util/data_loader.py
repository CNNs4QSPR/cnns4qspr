import torch
import os
import numpy as np
from scipy.stats import special_ortho_group

def voxelizer(pdf_fn):
    # Code that gets n_atoms, positions, atom_types and residue arrays from pdb

    return 0
